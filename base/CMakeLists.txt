file(GLOB HEADER_LIST CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/*.h")
file(GLOB LIBSRC_LIST CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/*.c")
add_library(SELKIELoggerBase ${LIBSRC_LIST})

target_include_directories(SELKIELoggerBase INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
